# Conformance vectors, in-page

The same pure core (geometry, state machine, quantizer, tempo estimator, chord classifier) that drives the UI replays conformance/vectors.json on demand. Ports to other platforms are judged by the same vectors in their own test runners.

![conformance.png](../media/conformance.png)

**Verified:** ✓ all assertions pass

*Generated by `tests/run.mjs` — edits here are overwritten; edit `tests/topics.mjs`.*
