# Keyboard navigation

Right-click (or m) opens the menu idle; arrows rotate the highlight, Enter commits, Escape backs out. Every wedge is a labelled menuitem for assistive tech, and each highlight effect carries its own label so what is announced can never belong to a different ring.

![keyboard-path.png](../media/keyboard-path.png)

**Verified:** ✓ all assertions pass · vectors v0.3.0 · 34 cases

*Generated by `scripts/build-docs.mjs` from a green `npm test` run — edits here are
overwritten; edit `tests/topics.mjs`.*
