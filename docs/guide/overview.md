# Graph canvas

The prototype ships a small map of the quaternionmedia stack. Drag nodes, drag the background to pan, pinch or wheel to zoom. Every mutation — including selection — is an intent logged with its input cost (⊙).

![overview.png](../media/overview.png)

**Verified:** ✓ all assertions pass

*Generated by `tests/run.mjs` — edits here are overwritten; edit `tests/topics.mjs`.*
