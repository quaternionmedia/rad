# Submenu by drag-through

Wedges with children open in place when the finger crosses the outer rim — still one gesture. Here: Color → swatch, committed on release at IPA 1. The swatch commits a palette token (color:calm), never a hex, so the same intent means the same thing under any theme.

![submenu-drag-through.png](../media/submenu-drag-through.png)

**Verified:** ✓ all assertions pass · vectors v0.3.0 · 34 cases

*Generated by `scripts/build-docs.mjs` from a green `npm test` run — edits here are
overwritten; edit `tests/topics.mjs`.*
